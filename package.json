{
  "name": "agentq-demo-cicd",
  "version": "1.0.0",
  "private": true,
  "description": "Demo: Playwright + AgentQ result reporting via API key in GitHub Actions",
  "scripts": {
    "test": "playwright test"
  },
  "dependencies": {
    "agentq-playwright": "^1.0.68"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1"
  }
}
